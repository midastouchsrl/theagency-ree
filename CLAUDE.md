> 🟡 **NOTA TEMPORANEA — DA RIMUOVERE ALLA RIATTIVAZIONE.**
> Il sito e **temporaneamente sospeso**: il deploy Vercel `theagency` e stato messo **in pausa** (2026-07-19),
> quindi i visitatori vedono la pagina automatica "deployment in pausa".
> **Per riattivarlo:** togli la pausa al progetto su Vercel (Dashboard -> progetto `theagency` -> Settings -> Resume,
> oppure `POST /v1/projects/{id}/unpause`). Nessun database o dato da ripristinare: e un sito statico.
> Quando il sito torna online, **cancella questa nota** cosi non resta stale.

@AGENTS.md
